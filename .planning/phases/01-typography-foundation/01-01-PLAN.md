---
phase: "01-typography-foundation"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified: ["src/styles/tailwind.css", "src/pages/index.astro"]
autonomous: false
must_haves:
  truths: ["Hero headline visually dominates the viewport at approximately 10vw on desktop", "Hero headline remains readable and contained at 200% browser zoom", "Hero fills the mobile viewport without scroll cutoff when address bar collapses", "Tagline and CTA are clearly secondary to the headline by proportional size contrast", "Hero text is left-aligned with editorial asymmetric positioning", "Headline wraps naturally to 2-3 lines on narrower viewports rather than shrinking"]
  artifacts: ["src/styles/tailwind.css provides fluid typography tokens containing clamp(3rem 10vw", "src/pages/index.astro provides updated hero section with svh height"]
  key_links: ["index.astro -> tailwind.css via text-hero utility class", "index.astro -> tailwind.css via svh viewport unit"]
---

<objective>
Transform the hero section from its current conservative typography (~4.5rem max) to viewport-dominating scale (~10vw on desktop) with fluid clamp() values, expand the container to let the headline breathe, switch to svh for mobile viewport stability, and establish clear visual hierarchy with left-aligned editorial layout.

Purpose: Deliver all 7 Phase 1 requirements (TYPO-01/02/03, LAYO-01/02/03/04) in one cohesive change since the CSS tokens and template markup are tightly coupled.
Output: Updated hero with massive headline, proportional tagline/CTA, expanded container, svh height, and editorial left-aligned composition.
</objective>

<execution_context>
@C:/Users/adria/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/adria/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-typography-foundation/01-CONTEXT.md
@.planning/phases/01-typography-foundation/01-RESEARCH.md
@src/styles/tailwind.css
@src/styles/custom.css
@src/pages/index.astro
@src/layouts/Base.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update fluid typography tokens and hero container width</name>
  <files>src/styles/tailwind.css</files>
  <action>
Update the `@theme` block in `src/styles/tailwind.css` to replace the current hero typography tokens with viewport-filling fluid values. Also expand the container utility.

**Replace these token values:**

1. `--text-hero`: Change from `clamp(2.75rem, 5vw + 1rem, 4.5rem)` to `clamp(3rem, 10vw, 9rem)`.
   - 3rem minimum ensures readability at extreme zoom/narrow viewports
   - 10vw preferred scales to viewport-filling on desktop
   - 9rem cap prevents absurdity on ultra-wide monitors
   - All bounds in rem for WCAG 1.4.4 zoom compliance (TYPO-02)

2. `--text-hero-tagline`: Change from `clamp(1.125rem, 1.5vw + 0.5rem, 1.375rem)` to `clamp(1.25rem, 3vw, 3rem)`.
   - Proportional to headline (roughly 3:1 ratio) creating clear hierarchy (LAYO-03)
   - Much larger than current tagline to maintain proportional relationship

3. Add a new token `--text-hero-cta` with value `clamp(1rem, 1.5vw + 0.5rem, 1.25rem)`.
   - CTA stays functional size, clearly tertiary in hierarchy
   - Place it after `--text-hero-tagline` in the responsive font sizes section

**Add new spacing tokens** (after existing spacing tokens):

4. `--spacing-hero-gap: clamp(1.5rem, 3vh, 3rem)` — gap between headline and tagline
5. `--spacing-hero-cta-gap: clamp(2rem, 4vh, 4rem)` — gap between tagline and CTA

**Update the container utility:**

6. In the `@utility container` block, change `max-width` from `1200px` to `clamp(900px, 80vw, 1400px)` (LAYO-01). This lets the hero headline breathe at full width while constraining on ultra-wides.

**Do NOT change** any non-hero tokens, base styles, or keyframe animations.
  </action>
  <verify>
Run `npm run build` to confirm no CSS syntax errors. Grep `src/styles/tailwind.css` for the updated values to confirm they are present: `10vw`, `clamp(3rem`, `--text-hero-cta`, `--spacing-hero-gap`, `clamp(900px`.
  </verify>
  <done>
tailwind.css contains: hero headline clamp with 10vw preferred value, hero tagline with 3vw proportional scale, hero CTA token, hero spacing tokens, and expanded fluid container width. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite hero section markup for editorial layout and svh height</name>
  <files>src/pages/index.astro</files>
  <action>
Update the hero section in `src/pages/index.astro` (the `if (d.section === "hero")` block, lines ~47-63) and its associated `<style>` block.

**Template changes (the hero JSX):**

1. **Hero section element:** Replace `min-h-screen` with `min-h-[100svh]` (LAYO-02). Keep `flex`, `overflow-hidden`, `relative`. Change `items-center` to `items-end` and add `pb-[clamp(4rem,8vh,8rem)]` to position content in the lower portion of the viewport for editorial asymmetry (LAYO-04). Remove `pt-34 pb-26` padding. Add `pt-24` to clear the nav.
   Result: `<section class="hero relative min-h-[100svh] flex items-end pt-24 pb-[clamp(4rem,8vh,8rem)] overflow-hidden">`

2. **Hero content wrapper:** Remove `max-w-[900px]` from `hero-content` div (LAYO-01 — container utility now handles width). Keep `relative z-1`.
   Result: `<div class="hero-content relative z-1">`

3. **Headline h1:** Change `font-extrabold` to `font-black` (weight 900 per user decision for "Black / Extra Bold 800-900" — font-black = 900, maximum commanding presence). Keep `font-heading text-hero leading-tight tracking-tighter`. Change `mb-8` to use the new spacing token: replace with `mb-[var(--spacing-hero-gap)]`. Add overflow safety via inline style `style="overflow-wrap: break-word; word-break: normal; hyphens: none;"` per user decision for long-word overflow handling.
   Result: `<h1 class="hero-title font-heading text-hero font-black leading-tight tracking-tighter mb-[var(--spacing-hero-gap)]" style="overflow-wrap: break-word; word-break: normal; hyphens: none;">{d.heading}</h1>`

4. **Tagline p:** Keep `hero-tagline text-text-muted` and `font-normal`. The `text-hero-tagline` class already references the updated token — no class change needed. Remove `max-w-[640px]` to let tagline flow naturally with the wider container. Change `mb-12` to `mb-[var(--spacing-hero-cta-gap)]`. Change `leading-loose` to `leading-relaxed` (1.6 instead of 1.7 — tighter at larger sizes reads better).
   Result: `<p class="hero-tagline text-hero-tagline text-text-muted mb-[var(--spacing-hero-cta-gap)] leading-relaxed font-normal">{d.subheading}</p>`

5. **CTA button (`.btn-primary` inside hero-actions):** Add `text-hero-cta` class to the anchor so the CTA uses the new proportional token. The utility class will override the hard-coded font-size in `.btn-primary` CSS since Tailwind utilities beat component styles.
   Result: `<a href={d.cta_link} class="btn-primary text-hero-cta">{d.cta_text}</a>`

**Style block changes (the `<style>` tag in index.astro):**

6. **Mobile override for `.hero`** (inside `@media (max-width: 768px)`): Change `min-height: 90vh` to `min-height: 100svh` for consistent mobile behavior (LAYO-02). Remove `padding: 7rem 0 4rem` (let the Tailwind classes handle it). Add `align-items: flex-end` to match desktop.

7. **Do NOT change** any non-hero styles, decorative effects (.hero-bg-glow, .hero-grid-pattern, .hero-title gradient), animations, or the process/problem/diff/basics/work/cta sections.

**Text is left-aligned by default** (no `text-center` class on hero), so LAYO-04 left-alignment is already satisfied. The `items-end` + bottom padding creates the editorial asymmetric positioning (content sits in the lower-left quadrant rather than dead center).
  </action>
  <verify>
Run `npm run build` to confirm no template syntax errors. Visually inspect the hero section classes in the built HTML to confirm svh, font-black, hero-cta, and spacing-hero values appear.
  </verify>
  <done>
Hero section uses 100svh height, font-black weight, proportional spacing tokens, no max-width constraint on hero-content, editorial bottom-aligned layout, and overflow-wrap safety. Build passes without errors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of hero typography and layout</name>
  <files>src/pages/index.astro</files>
  <action>
No automated changes. This is a human visual verification checkpoint for the hero redesign completed in Tasks 1-2.
  </action>
  <what-built>
Viewport-dominating hero typography with:
- Headline at ~10vw scale (dramatically larger than before)
- Proportional tagline (~3vw) and CTA sizing
- Left-aligned, bottom-positioned editorial layout
- Full svh viewport height
- Fluid container width (900px-1400px)
- Font weight 900 (black)
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:4321
2. **Desktop (1440px wide):** Headline should be massive (~144px), dominating the viewport. Text left-aligned, positioned in lower portion of screen. Tagline clearly smaller. CTA button proportionally sized.
3. **Resize to 768px:** Headline should wrap to 2-3 lines but remain the dominant element. No horizontal overflow or scrollbar.
4. **Resize to 375px (mobile):** Hero fills full screen height. Headline still largest element. No horizontal overflow.
5. **200% zoom test:** At 1440px zoomed to 200%, headline should shrink to fit (rem min kicks in at 3rem = 48px at default, scales with zoom). No overflow, no scrollbar.
6. **Check both themes:** Toggle light/dark — gradient text treatment should still work on the headline.
7. **Scroll down:** Other sections should look unchanged.
  </how-to-verify>
  <verify>Human confirms visual appearance matches expectations.</verify>
  <done>User approves the hero typography scale, layout positioning, visual hierarchy, and responsive behavior.</done>
  <resume-signal>Type "approved" or describe any issues (sizing, spacing, overflow, hierarchy concerns)</resume-signal>
</task>

</tasks>

<verification>
- Build completes without errors: `npm run build`
- Hero headline uses clamp(3rem, 10vw, 9rem) — visible in computed styles at ~10vw on 1440px desktop
- Hero uses 100svh — no address bar cutoff on mobile
- Container width is fluid clamp(900px, 80vw, 1400px)
- No horizontal overflow at any viewport width from 320px to 2560px
- 200% zoom: headline contained, no overflow
- Visual hierarchy: headline >> tagline >> CTA by size ratio (~3:1 headline-to-tagline)
- Left-aligned text with bottom-positioned editorial layout
- Existing gradient text, glow effects, and animations preserved
- All non-hero sections unchanged
</verification>

<success_criteria>
1. Hero headline appears at viewport-filling scale (~10vw preferred) on desktop — TYPO-01
2. Headline scales with browser zoom via rem bounds in clamp() — TYPO-02
3. Headline scales from mobile (3rem min) to ultra-wide (9rem max) without breaking — TYPO-03
4. Hero content container expanded beyond 900px (now fluid to 1400px) — LAYO-01
5. Hero uses svh for mobile viewport stability — LAYO-02
6. Tagline and CTA clearly secondary by proportional size contrast — LAYO-03
7. Left-aligned, bottom-positioned editorial asymmetric layout — LAYO-04
</success_criteria>

<output>
After completion, create `.planning/phases/01-typography-foundation/01-01-SUMMARY.md`
</output>
